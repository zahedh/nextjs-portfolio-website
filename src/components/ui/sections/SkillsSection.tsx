import { Section } from '@/components';
import { en } from '@/language';

export default function SkillsSection() {
  return (
    <Section anchor="skills" title={en.sectionHeaders.skillsCollage}></Section>
  );
}
